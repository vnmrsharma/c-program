// Take Input from user and print it

#include <stdio.h>

int main(){

    //if input is a word
    char str[50];
    printf("Enter a word:");
    scanf("%s",str);
    printf("Entered word is %s",str);

    return 0;
}
